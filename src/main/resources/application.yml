spring:
  application:
    name: service-a
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
    netflix:
      eureka:
        client:
          service-url:
            defaultZone: http://localhost:8761/eureka/
  webflux:
    enabled: true